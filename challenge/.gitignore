# Report generati dai gruppi (runtime)
outputs/*.html

# Modelli serializzati (consegnati dagli studenti — non in repo)
outputs/*.joblib
submissions/

# Test set nascosto (solo docente, non visto dagli studenti)
datasets/*_test.csv

# Cheat-sheet docente (baseline + domande, NON visibile agli studenti)
instructor_cheatsheet.json
